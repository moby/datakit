(jbuild_version 1)

(library
  ((name      test)
   (wrapped   false)
   (libraries (alcotest mtime mtime.clock.os logs.fmt io-page.unix mirage-flow
               datakit-client datakit-server datakit datakit_io))))
